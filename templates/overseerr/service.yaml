apiVersion: v1
kind: Service
metadata:
  name: overseerr
  namespace: default
  labels:
    app.kubernetes.io/name: overseerr
spec:
  type: ClusterIP
  ports:
    - port: 5055
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: overseerr
